# Run npm run setup to generate a private admin token in .env.
ADMIN_TOKEN=
HOST=127.0.0.1
PORT=4318
DATABASE_PATH=./data/calendar.sqlite
# Production: https://static.jieqi.dev (leave empty for same-origin local use)
STATIC_ORIGIN=
